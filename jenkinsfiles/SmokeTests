pipeline {
    agent any
    tools {
            maven 'Maven 3.8.4'
        }

    stages {
        stage('Run tests') {
            steps {
                sh 'mvn clean verify -Denv=QA'
            }
        }
    }
}